name: shared_theme
description: Katuya brand colors, typography, and shared UI components
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_models:
    path: ../shared_models

flutter:
