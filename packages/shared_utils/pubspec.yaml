name: shared_utils
description: Katuya cross-app helpers — Firebase providers, location, date, validators
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_messaging: ^14.7.10
  firebase_storage: ^11.6.0
  flutter_riverpod: ^2.4.9
  intl: ^0.19.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  geofire_common: ^1.0.1

flutter:
