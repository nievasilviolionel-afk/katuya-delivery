name: shared_models
description: Katuya shared domain models with Freezed and JSON serialization
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  cloud_firestore: ^4.14.0

flutter:

